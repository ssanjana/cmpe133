<<<<<<< HEAD
<%if user_signed_in? %>
    <div class="container-fluid">
      <!-- <h1>History Page</h1>     --> 
      <p> </p>
      <p>You have made the following purchases recently...</p>       
    </div>
    <%# ***************Items Grid form *****************
      From 7 Creating Books(Products) YouTube video
        - Due to it being the products index,
        - This should be a loop to display all products
        - in rows of 3
        ************************************************ %>

    <% for cart in @carts %>
      <div class="card-info-container">
        <%=image_tag('white.jpg', alt: 'white', height: 40, width: 50) %>
      </div>
      </br>
      <div class="shadowed-card">
        Bought at <td><%= h cart.created_at %></td>

        <% for line_item in cart.line_items %>
          <div class="row">
            <%=image_tag('white.jpg', alt: 'white', height: 20, width: 30) %>
            <div class="card-container">
              <div class="shadowed-card">
     	          <%= link_to image_tag(line_item.product.image_url, alt: line_item.product.title, height: 200, width: 300), line_item %>
	              <div class="title"><%= link_to line_item.product.title, line_item.product %></div>
                <div class="price"><%= number_to_currency line_item.product.price %></div>
                <div class="product-info" >
                  <%= button_to 'Buy it Again', line_items_path(product_id: line_item.product), {:class => "add-to-cart-button" } %> 
                </div>
              </div>
	          </div>
	        </div>
        <%end%>

      </div>
    <%end%>
    

<% else %>
You need to be a registered member to use our history.
<%end%>
=======
<%if user_signed_in? %>
    <div class="container-fluid">
      <!-- <h1>History Page</h1>     --> 
      <p> </p>
      <p>You have made the following purchases recently...</p>       
    </div>
    <%# ***************Items Grid form *****************
      From 7 Creating Books(Products) YouTube video
        - Due to it being the products index,
        - This should be a loop to display all products
        - in rows of 3
        ************************************************ %>

    <% for cart in @carts %>
      <div class="card-info-container">
        <%=image_tag('white.jpg', alt: 'white', height: 40, width: 50) %>
      </div>
      </br>
      <div class="shadowed-card">
        Bought at <td><%= h cart.created_at %></td>

        <% for line_item in cart.line_items %>
          <div class="row">
            <%=image_tag('white.jpg', alt: 'white', height: 20, width: 30) %>
            <div class="card-container">
              <div class="shadowed-card">
     	          <%= link_to image_tag(line_item.product.image_url, alt: line_item.product.title, height: 200, width: 300), line_item %>
	              <div class="title"><%= link_to line_item.product.title, line_item.product %></div>
                <div class="price"><%= number_to_currency line_item.product.price %></div>
                <div class="product-info" >
                  <%= button_to 'Buy it Again', line_items_path(product_id: line_item.product), {:class => "add-to-cart-button" } %> 
                </div>
              </div>
	          </div>
	        </div>
        <%end%>

      </div>
    <%end%>
    

<% else %>
You need to be a registered member to use our history.
<%end%>
>>>>>>> 4933270bbf7bc821417be985da244c8d8dadcc30
